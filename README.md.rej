diff a/README.md b/README.md	(rejected hunks)
@@ -40,9 +43,9 @@ NLP tools for qualifications and distinctions
 ### Command Line Interface
 
 The qualo command line tool is automatically installed. It can
-be used from the shell with the `--help` flag to show all subcommands:
+be used from the console with the `--help` flag to show all subcommands:
 
-```shell
+```console
 qualo --help
 ```
 
@@ -55,15 +58,15 @@ qualo --help
 The most recent release can be installed from
 [PyPI](https://pypi.org/project/qualo/) with:
 
-```shell
-pip install qualo
+```console
+python3 -m pip install qualo
 ```
 -->
 
 The most recent code and data can be installed directly from GitHub with:
 
-```shell
-pip install git+https://github.com/cthoyt/qualo.git
+```console
+python3 -m pip install git+https://github.com/cthoyt/qualo.git
 ```
 
 ## 👐 Contributing
